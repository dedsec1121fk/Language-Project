# Language Modules

Every executable candidate under `languages/<id>/` is now self-contained: worker source, native practical tool source, metadata, README, and reproducible example assets are stored together. This makes individual language contributions easier to inspect, maintain, and test.
