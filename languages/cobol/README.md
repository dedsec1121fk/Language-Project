# COBOL

Termux package(s): `gnucobol`.

This module provides practical source and runtime-detection metadata. It is part of the full Termux language coverage layer; it is not falsely counted as an active reversible worker until a persistent worker protocol implementation is verified.
