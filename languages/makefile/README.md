# Make

Termux package(s): `make`.

This module represents **Make** in the full Termux language coverage layer. It contains a practical source artifact intended for local, deterministic use or syntax/runtime verification.

GitHub Linguist separate-language support: **yes**.
